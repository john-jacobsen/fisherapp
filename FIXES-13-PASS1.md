"You are rewriting all lesson content for Fisher App. Your job is to regenerate backend/data/lessons/ markdown files at a much higher quality level. Read the file backend/scripts/generate_lessons.py to understand the current structure. Then replace the content for ALL 30 foundation nodes (prefixes: frac-, order-, exp-, eq-, log-, sum-, comb-, geo-). Write directly to backend/data/lessons/{node-id}.md for each node. Each lesson MUST follow this exact structure: 1) # Title, 2) ## Overview with bolded key term, 3) ## Key Idea with the central formula in display math, 4) ## Worked Examples with exactly 3 examples — each showing the problem, step-by-step solution explaining WHY each step works (not just what to do), and final answer, 5) ## Common Mistakes with 2-3 real student errors, 6) ## Quick Check with 3 problems and answers. Use dollar signs for inline math and double dollars for display math. Tone: direct, clear, second person. Assume prereqs are mastered. 300-500 words per lesson. CRITICAL RULES: Never use the word 'likelihood' as a synonym for 'probability' — that word has a specific technical meaning introduced later. For log-rules: answers to examples should stay in LOG FORM (e.g., log_3(20)), NOT computed numerical values. The pedagogical point is manipulating log expressions, not evaluating them. Use these 3 examples as your quality benchmark (read them carefully): backend/data/lessons/frac-simplify.md should match this level..." then paste the frac-simplify, log-rules, and prob-bayes samples from the file sample-lessons-v2.md in the outputs folder at /mnt/user-data/outputs/sample-lessons-v2.md. After writing all 30 files, verify each is at least 800 bytes and contains all required sections."